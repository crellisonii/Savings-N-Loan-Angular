export enum SimpleInterestFrequency {
  YEARLY = 1,
  MONTHLY = 12,
}

export enum LengthType {
  YEARLY = 'yearly',
  MONTHLY = 'monthly',
}

export enum CompoundFrequency {
  YEARLY = 1,
  MONTHLY = 12,
  DAILY = 365,
}
